document.addEventListener('DOMContentLoaded',() => {
    // const user_uuid = localStorage.getItem('behavioral-experiment-user-id');

    // const user_fields = document.querySelectorAll("input[name='user_uuid']");
    
    // user_fields.forEach(field => {
    //     field.value = user_uuid;
    // });
});